@page
@model AccessDeniedModel
@{
    ViewData["Title"] = "Access Denied";
}

<div class="access-denied-wrapper fade-in-1">
    <div class="grid-item card-pink">

        <h1>@ViewData["Title"]</h1>

        <p>You do not have access to this page or resource.</p>

        <p>Please return to the home page.</p>

        @if (User.IsInRole("Guest"))
        {
            <div class="action-buttons">
                <a asp-page="/Parties/Index" class="btn-primary btn-hero">
                    Home
                </a>
            </div>
        }

        @if (User.IsInRole("Server") || User.IsInRole("Host"))
        {
            <div class="action-buttons">
                <a asp-page="/Index" class="btn-primary btn-hero">
                    Home
                </a>
            </div>
        }

    </div>
</div>
