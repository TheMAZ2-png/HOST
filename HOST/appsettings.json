{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*",

  "ConnectionStrings": {
    // ❗ MUST be emptied before pushing to GitHub
    // Replace with "" or move to User Secrets
    "HOST": ""
  },

  "MongoDBSettings": {
    // ❗ MUST be emptied before pushing
    "ConnectionString": "",
    "DatabaseName": "HOST",
    "CollectionName": "curriculum"
  },

  // ⭐ Safe placeholder — no secrets here
  "Twilio": {
    "AccountSid": "",
    "AuthToken": "",
    "FromPhoneNumber": ""
  },

  // ⭐ Safe placeholder — no secrets here
  "AISettings": {
    "ApiKey": "",
    "Model": "gemini-2.5-flash",
    "BaseAddress": "https://generativelanguage.googleapis.com/v1beta/"
  }
}
